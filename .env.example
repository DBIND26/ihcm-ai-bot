# Required — API calls fail without this
ANTHROPIC_API_KEY=sk-ant-...

# Optional — app works with static fallbacks if these are missing
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_KEY=eyJ...
